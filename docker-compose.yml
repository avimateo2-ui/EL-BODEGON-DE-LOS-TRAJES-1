services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: defintivo_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
